console.info(
  [
    "Hedera topic creation stub.",
    "This command intentionally performs no network calls and submits no transaction.",
    "Implement explicit confirmation and transaction signing before enabling writes.",
  ].join("\n"),
);
