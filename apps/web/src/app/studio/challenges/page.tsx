import { ChallengeManager } from "../../../components/studio/challenge-manager";

export const metadata = { title: "Manage challenges — STAGE" };

export default function StudioChallengesPage() {
  return <ChallengeManager />;
}
