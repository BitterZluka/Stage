import { ComingSoon } from "../../components/coming-soon";

export const metadata = { title: "Creator Studio — STAGE" };

export default function StudioPage() {
  return (
    <ComingSoon
      title="Creator Studio is coming soon"
      description="Launching challenges, publishing perks, and managing submissions will live in the authenticated Creator Studio."
    />
  );
}
