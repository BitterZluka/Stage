import { PerkManager } from "../../../components/studio/perk-manager";

export const metadata = { title: "Manage perks — STAGE" };

export default function StudioPerksPage() {
  return <PerkManager />;
}
