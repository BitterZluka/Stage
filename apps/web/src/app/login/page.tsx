import { ComingSoon } from "../../components/coming-soon";

export const metadata = { title: "Log in — STAGE" };

export default function LoginPage() {
  return (
    <ComingSoon
      title="A dedicated login page is coming soon"
      description="For now, use the Log in button in the header to open the login dialog."
    />
  );
}
